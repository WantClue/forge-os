name: Build & Release Firmware [Pre-Release]

on:
  release:
    types: [prereleased]

permissions:
  checks: write
  pull-requests: write
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v4
      with:
        submodules: 'recursive'
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.12.0'
    - name: Build web dist
      working-directory: ./main/http_server/forge-os
      run: |
        npm ci
        npm run build
    - name: esp-idf build
      uses: espressif/esp-idf-ci-action@v1
      with:
        esp_idf_version: v5.4
        target: esp32s3
        command: GITHUB_ACTIONS="true" idf.py build
        path: '.'
    - uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'
    - run: pip install esptool
    - name: "Create factory/merged image"
      run: "./merge_bin.sh ./bitforgeos-merged.bin"
    - name: upload bitforgeos-merged.bin
      uses: actions/upload-artifact@v4
      with:
        name: bitforgeos-merged.bin
        path: ./bitforgeos-merged.bin
    - name: upload bitforgeos.bin
      uses: actions/upload-artifact@v4
      with:
        name: bitforgeos.bin
        path: ./build/bitforgeos.bin
    - name: upload www.bin
      uses: actions/upload-artifact@v4
      with:
        name: www.bin
        path: ./build/www.bin
    - name: Release firmware assets
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/')
      with:
        prerelease: true
        files: |
          ./build/bitforgeos.bin
          ./build/www.bin
